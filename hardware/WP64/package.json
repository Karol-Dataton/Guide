{
    "name": "watchpax64-wiki",
    "version": "1.0.0",
    "description": "WATCHPAX 64 User Guide - Documentation Wiki",
    "scripts": {
        "build": "node scripts/setup-polish-locale.js --prepare && node scripts/build-content.js && CONTENT_DIR=content-pl OUTPUT_DIR=pl node scripts/build-content.js && node scripts/generate-static.js && SITE_DIR=pl LOCALE=pl node scripts/generate-static.js && node scripts/setup-polish-locale.js --sync",
        "extract": "node scripts/extract-to-markdown.js",
        "pdf": "node scripts/generate-pdf.js",
        "release": "node scripts/release.js",
        "bump": "npm run release"
    },
    "keywords": [
        "watchpax64",
        "documentation",
        "wiki"
    ],
    "license": "UNLICENSED"
}
